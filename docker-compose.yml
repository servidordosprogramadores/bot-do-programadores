services:
  bot:
    image: ghcr.io/matheusaudibert/bot-dos-programadores:latest
    container_name: bot-dos-programadores
    restart: unless-stopped
    env_file:
      - .env
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
