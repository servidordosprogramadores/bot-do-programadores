services:
  bot:
    build: .
    container_name: bot-dos-programadores
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PORT:-80}:${PORT:-80}"
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
